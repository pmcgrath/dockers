See http://docs.docker.io/en/latest/installation/ubuntulinux/
For an existing ubuntu 12.04
	# Upgrade to a 3.8 kernel which is a requirement
	sudo apt-get update
	sudo apt-get -y install linux-image-generic-lts-raring linux-headers-generic-lts-raring
	sudo reboot

	# Install docker
	sudo apt-key adv --keyserver keyserver.ubuntu.com --recv-keys 36A1D7869245C8950F966E92D8576A8BA88D21E9
	sudo sh -c "echo deb http://get.docker.io/ubuntu docker main > /etc/apt/sources.list.d/docker.list"
	sudo apt-get update
	sudo apt-get -y install lxc-docker

	# Verify docker version
	sudo docker version

	# Pending
	# Sort out docker group membership so we do not need to use sudo everywhere ?

	# Ufw
	# Not sure about this as seems to be working at the moment without this change
	sudo vim /etc/default/ufw
		----
		# Change:
		# DEFAULT_FORWARD_POLICY="DROP"
		# to
		DEFAULT_FORWARD_POLICY="ACCEPT"
		----
	sudo ufw reload

	# Almost certain i do not need this unless i'm controlling the containers on another machine
		UFW’s default set of rules denies all incoming traffic. If you want to be able to reach your containers from another host then you should allow incoming connections on the Docker port (default 4243):
		sudo ufw allow 4243/tcp


